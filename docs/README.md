# `docs/`

This directory is **empty for now**—it was not part of the plan to ship extra documentation here yet. The root [README.md](../README.md) and the [publish guide](../guides/publish-extension.md) are enough to build and use the project.

(You can add subfolders or notes here later; anything under `private/` is gitignored if you use that name—see the root [`.gitignore`](../.gitignore).)
